import { TableExportCopyDirective } from './table-export-copy.directive';

describe('TableExportCopyDirective', () => {
  it('should create an instance', () => {
    const directive = new TableExportCopyDirective();
    expect(directive).toBeTruthy();
  });
});
